<ul class="top-menu" id="top-menu">
  <li>
    <a class="top-menu-item" href="/news">НОВОСТИ</a>
    <span class="divider">/</span>

    <div class="sub-top-menu">
      <div class="sub-top-menu-inner">
        <ul>
          <% @menu_categories.each do |category| %>
              <li>
                <%= link_to category.title, "/#{category.short_url}" %>
              </li>
          <% end %>

          <% if can?(:create, @categories) %>
              <li>
                <%= link_to(t('helpers.submit.create', :model => t('activerecord.models.category')), new_category_path) %>
              </li>
          <% end %>
        </ul>
      </div>
    </div>
  </li>
  <li>
    <a class="top-menu-item" href="/albums">Фото</a>
    <span class="divider">/</span>

    <div class="sub-top-menu">
      <div class="sub-top-menu-inner">
        <ul>
          <% @menu_categories.each do |category| %>
              <li>
                <%= link_to category.title, "/photo/#{category.short_url}" %>
              </li>
          <% end %>
        </ul>
      </div>
    </div>
  </li>
  <li>
    <a class="top-menu-item" href="/community">Сообщество</a>
    <span class="divider">/</span>

    <div class="sub-top-menu">
      <div class="sub-top-menu-inner">
        <ul>
          <li><a href="/">Блоги</a></li>
          <li><a href="/">Кафедры</a></li>
          <li><a href="/">Преподаватели</a></li>
        </ul>
      </div>
    </div>
  </li>
  <li class="last">
    <a class="top-menu-item" href="/press/about">Газета</a>

    <div class="sub-top-menu">
      <div class="sub-top-menu-inner">
        <ul>
          <% @menu_statics.each do |static| %>
              <li>
                <%= link_to static.title, "/press/#{static.short_url}" %>
              </li>
          <% end %>
        </ul>
      </div>
    </div>
  </li>
  <li class="logo-mendeleevec">
  </li>
</ul>
